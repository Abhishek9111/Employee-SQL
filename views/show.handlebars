<table class="table table-striped">
    <thead>
        <tr>
            <th>Full Name</th>
            <th>Email</th>
            <th>Mobile</th>
            <th>City</th>
            <th></th>
            <th><a href='addEmp' class="btn btn-info">Add Employee</a></th>
        </tr>
        <tbody>
            {{#each list}}
            <tr>
            <td>{{this.name}}</td>
            <td>{{this.email}}</td>
            <td>{{this.phone}}</td>
            <td>{{this.city}}</td>
            <td><a href="/delete/{{this.id}}" class="btn btn-danger">Delete</a></td>
            <td><a href="/update/{{this.id}}" class="btn btn-info">Update</a></td>
            </tr>
            {{/each}}

        </tbody>
    </thead>
</table>